[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers --tb=short
markers =
    live: requires real API keys and network access (auto-skipped without GOOGLE_API_KEY)
filterwarnings =
    ignore::DeprecationWarning
